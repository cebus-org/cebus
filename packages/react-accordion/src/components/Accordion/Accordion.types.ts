import type {
  AccordionProps as FluentAccordionProps,
  AccordionState as FluentAccordionState,
  AccordionSlots as FluentAccordionSlots,
} from '@fluentui/react-accordion';

export type AccordionSlots = FluentAccordionSlots;

export type AccordionProps = FluentAccordionProps;

export type AccordionState = FluentAccordionState;
